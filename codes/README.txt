# CS145

# decisionTree.py:
This module is used to construct, train and test decision tree model and random forest model. It takes in the training vectors file
"../data/out/clf_train.vectors" and the training label file "../data/out/clf_train.labels". It will output four predicted classes for both
decision tree and random forest model. 
